machine:
  timezone:
    Asia/Tokyo
  node:
    version: 4.3
dependencies:
  pre:
    - 'echo "npm install ..."'
  override:
    - 'echo "npm install ..."'
test:
  pre:
    - 'echo "****** Start Test *****"'
  override:
    - 'echo "test"'
  post:
    - 'echo "****** End Text *****"'
deployment:
  deploy:
    branch:
      master
    commands:
      - 'echo "+++++ Start Deploy +++++"'
      - 'echo "+++++ End Deploy +++++"'
      - 'echo 1'
